fragment VariantDetails on ProductVariant {
  id
  name
  quantityAvailable
  weight {
    unit
    value
  }
  attributes {
    attribute {
      slug
      name
    }
    values {
      slug
      name
    }
  }
  pricing {
    price {
      gross {
        currency
        amount
      }
    }
  }
}
